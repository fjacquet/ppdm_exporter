apiVersion: v1
kind: Service
metadata:
  name: {{ include "ppdm-exporter.fullname" . }}
  labels:
    {{- include "ppdm-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ppdm-exporter.selectorLabels" . | nindent 4 }}

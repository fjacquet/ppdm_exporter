# EnvironmentFile for the ppdm_exporter systemd unit.
# Supplies server secrets referenced as ${VAR} in config.yaml.
#
# Install:  /etc/ppdm_exporter/ppdm_exporter.env   (chmod 0600, chown root:ppdm)

PPDM1_PASSWORD=change-me
# PPDM2_PASSWORD=change-me
